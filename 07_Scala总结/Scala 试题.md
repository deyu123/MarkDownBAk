# Scala 试题

### 1. 相关试题

```sql
1.对于拉链操作val t1 = Array(1, 3); val t2 = Array(2,4) ; t1.zip(t2)返回结果正确的是？(2分)
A、（1,2,3,4)
B、（(1,3),(2,4))
C、（(1,2),(3,4))
D、（(1,4),(3,4))
2.Scala关于变量定义、赋值，正确的是？(2分)
A、val a = 3
B、val a:String = 3
C、var b:Int = 3 ; b = 6
D、var b = "Hello World!" ; b = "123"
3.Scala关于for用于循环推导的关键字是？(2分)
A、yield
B、yeild
C、yelid
D、yleid
4.Scala里判断某个对象是否属于某个类用的方法是？(2分)
A、asInstanceOf[T]
B、isInstanceOf[T]
C、instanceOf
D、InstanceOf[T]
5.子类混入多个特质时候，构造器执行顺序，以下说法错误的是？(2分)
A、首先调用超类的构造器
B、特质构造器在超类构造器之后、类构造器之前执行
C、特质由右到左被构造
D、每个特质中，父特质先被构造
6.Scala中方法和函数的区别，以下说法错误的是？(2分)
A、方法和函数的定义语法不同
B、方法是面向对象编程的概念
C、函数本质是对象，可以作为参数传递给另一个函数或方法
D、方法不能转变成函数
7.关于主构造器，以下说法错误的是？(2分)
A. 辅助构造器的必须调用主构造器 B. 辅助构造器创建时候，调用主构造方法放在第一行 C. 辅助构造器创建时候，调用主构造方法不能放在第一行
D. 辅助构造器的名称和类名相同
8.Scala关于定义特质的语法？(2分)
A、Interface
B、interface
C、Trait
D、trait
9.高阶函数是指？(2分)
A. 在程序中应该首先被定义的函数。
B. 将函数作为参数，并返回结果为函数的函数。
C. 函数参数为函数或返回结果为函数的函数。
D. 执行时间长的函数。
10.下面有Option类型描述有误的是？(2分)
A. Option类型可以用来表示那种可能存在、也可能不存在的值。
B. Option类型的两种状态分别是Some和None。
C. Option类型的两种状态分别是Success和Failure。
D. 可以使用isEmpty方法来判断是否存在值。
二、简答题（50分）
1.var和val的区别。（10）
var修饰字段，值可变（4分）
val修饰字段，值不可变（4分）
在val和var均可的条件下，官方推荐使用val（2分）
2.谈谈你对伴生对象的认识。（10）
类和伴生对象之间可以相互访问私有的方法和属性（4分）
伴生对象不能访问被[this]修饰的属性（4分）
在java中经常用到在一个类中既有普通方法，又有静态方法（static修饰的方法），在scala中遇到这种情况，可以使用伴生对象来解决（2分）
3.请说明继承关系下，子类混入多个特质，构造器的执行顺序。（10）
首先调用超类的构造器（1分）
特质构造器在超类构造器之后、类构造器之前执行（1分）
特质由左到右被构造（2分）
每个特质中，父特质先被构造（2分）
如果多个特质共有一个父特质，那么那个父特质已经被构造，则不会被再次构造（2分）
所有特质构造完毕后，子类被构造（2分）
4.apply函数的意义和使用（举例说明）。（10）
apply方法是伴生对象中的一个方法（5分）。
apply方法常用于给类中参数注入值（5分）。
5.简写方法和函数的区别（10）
方法和函数的定义语法不同（2分）。
方法是面向对象编程的概念（2分）。
函数是函数式编程语言中“头等公民”（2分）。
函数本质是对象，可以作为参数传递给另一个函数或方法。（2分）。
方法可以转变成函数（2分）。
三、	编程题（30分）
1.生成一个list1，其中的数值是从1至100的整数，基于list1，生成list2，要求list2的数值是从1至100的偶数。（10）
val list=(1 to  100).filter(x=>{x%2==0})
2.使用模式匹配，实现，输入1，输出“red”;输入2，输出“green”输入3，输出“yellow”,并考虑匹配不成功的情况。（10）
def myCalc(a:String):Unit={
    //匹配1,2,3,_
    c match {
      case "1" => {print("red")}
      case "2"=> {print("green")}
      case "3"=> {print("yellow")}
      case _ => {print("匹配不成功！")}
    }
  }
3.读本地文件，scala实现WordCount。（10分）
//第一步：读取本地文件
var source: BufferedSource = Source.fromFile("D:/shuju/a.txt")
//第二步：调用getLine方法返回一个迭代器
var iter:Iterator[String] = source.getLines()
//第三步：wordcount统计
iter.toList.flatMap(x=>{x.split(" ")}).map(x=>{(x,1)}).groupBy(x=>{x._1}).mapValues(x=>{x.size})

```

